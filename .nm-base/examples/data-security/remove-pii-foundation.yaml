name: remove-pii-foundation
description: A foundation pipeline that passes through raw event data, including various PII.
type: pipeline

config:
  input:
    http_server:
      address: 0.0.0.0:8080
      path: /events/ingest

  pipeline:
    processors:
      - mapping: |
          root = this
          root.processed_at = now()

  output:
    stdout:
      codec: lines

pipeline:
  processors:
    - mapping: |
        # Drop DEBUG messages and health checks
        if this.level == "DEBUG" {
          root = deleted()
        }
        if this.message.contains("Health check passed") {
          root = deleted()
        }

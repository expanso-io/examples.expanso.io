name: first-result-remove-pii
type: pipeline
config:
  input:
    generate:
      count: 1
      interval: 1s
      mapping: |
        root = {"event_id":"demo-001","email":"alex@example.invalid","ip_address":"192.0.2.10","payment_card":"SYNTHETIC-CARD","action":"checkout"}
  pipeline:
    processors:
      - mapping: |
          root.event_id = this.event_id
          root.action = this.action
  output:
    stdout:
      codec: lines

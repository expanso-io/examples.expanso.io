---
title: PII removal configuration
sidebar_label: Full configuration
sidebar_position: 9
description: Inspect and copy the canonical YAML used by the PII removal walkthrough.
keywords: [pii, yaml, configuration, privacy]
contentArchetype: reference
executionStatus: offline-runnable
operationalEvidence: not-assessed
claimIds: []
claimsVerifiedBy: codex/content-claims-verifier-v1
claimsVerifiedAt: '2026-07-18'
claimsPolicyDigest: sha256:074699c58653998ab3cc614347cff3fa995c7651028692763a2b4d8a84d1a281
---

import CodeBlock from '@theme/CodeBlock';
import pipelineYaml from '!!raw-loader!../../../examples/data-security/remove-pii-complete.yaml';
import CopyPipelineButton from '@site/src/components/CopyPipelineButton';

# PII removal configuration

This is the canonical configuration referenced by the Explorer and machine journey. Review every input, processor, secret, and output before adapting it.

<CopyPipelineButton pipeline={pipelineYaml} />

<CodeBlock language="yaml" title="remove-pii.yaml">
  {pipelineYaml}
</CodeBlock>

## Limits

The machine fixture gate executes this exact `config` with one synthetic input, fixed test salts, the pinned Benthos image, and an exact expected JSONL output. That single core-path check does not establish deployment readiness, broad input coverage, failure behavior, or an operating envelope. The Explorer remains a curated walkthrough.

[Return to the interactive Explorer](/data-security/remove-pii/explorer).

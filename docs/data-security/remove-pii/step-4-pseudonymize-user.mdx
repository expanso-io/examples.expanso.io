---
title: 'Step 4: Replace the user name'
sidebar_label: 'Step 4: Replace user name'
sidebar_position: 7
description: Derive a shortened pseudonymous identifier and remove the source name.
keywords: [pii-removal, pseudonymization, user-name, mapping]
contentArchetype: step
executionStatus: offline-runnable
operationalEvidence: not-assessed
claimIds: []
claimsVerifiedBy: codex/content-claims-verifier-v1
claimsVerifiedAt: '2026-07-18'
claimsPolicyDigest: sha256:074699c58653998ab3cc614347cff3fa995c7651028692763a2b4d8a84d1a281
---

# Step 4: Replace the user name

Derive an identifier from the source name, remove the name, then attach the identifier to the new root object.

```yaml title="User-name mapping"
- mapping: |
    root = this.without("user_name")

    root.user_id = "user_" + this.user_name.hash(
      "hmac_sha256",
      env("USER_SALT")
    ).encode("hex").slice(0, 12)
```

Truncation increases collision risk, and names are weak source identifiers. Evaluate both properties before using this pattern for joins.

[Next: reduce location precision](/data-security/remove-pii/step-5-generalize-location).

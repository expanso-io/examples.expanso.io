---
title: 'Advanced Encryption Patterns'
sidebar_label: 'Step 5: Advanced Patterns'
sidebar_position: 8
description: Review unverified variants for multiple keys, audit metadata, and key versions.
keywords: [encryption, advanced-patterns, multi-key, audit]
contentArchetype: step
executionStatus: architecture-only
operationalEvidence: not-assessed
localNavigation: true
uniqueTasks: [review-encryption-patterns-step-5-advanced-patterns]
claimIds: []
claimsVerifiedBy: codex/content-claims-verifier-v1
claimsVerifiedAt: '2026-07-18'
claimsPolicyDigest: sha256:074699c58653998ab3cc614347cff3fa995c7651028692763a2b4d8a84d1a281
---

# Advanced Encryption Patterns

The earlier patterns illustrate field selection. These advanced sketches add key-version and transformation metadata; they still require an external key system, access model, and execution tests.

For a more detailed guide on these topics, please see the **[Advanced Encryption Patterns in the Encrypting Data tutorial](/data-security/encrypt-data/step-4-advanced-patterns)**.

## Pattern 1: Multi-Key Strategy

Using a single encryption key for all data is risky. A better pattern is to use separate keys for different data types based on their risk level. This is known as **risk-based encryption**.

- **`CARD_ENCRYPTION_KEY`**: For PCI-DSS data. Highest security, most frequent rotation.
- **`PII_ENCRYPTION_KEY`**: For GDPR/CCPA/HIPAA data (SSN, email). High security.
- **`ADDRESS_ENCRYPTION_KEY`**: For location data.
- **`TEMPORAL_ENCRYPTION_KEY`**: For sensitive dates.

This strategy limits the "blast radius" if a single key is ever compromised. You implement this by simply calling the `encrypt_aes` function with the appropriate key environment variable for each field, as demonstrated in the previous steps.

## Pattern 2: Transformation audit metadata

The following processor emits authored metadata about which transformation ran and why. A real audit design needs an independently protected destination, access rules, retention, and tested failure handling.

```yaml title="Add an Audit Log Processor"
- mapping: |
    root = this

    # Create an audit log entry
    let audit_log = {
      "event_id": uuid_v4(),
      "action": "ENCRYPT_PII",
      "timestamp": now(),
      "fields_encrypted": [
        "payment.card_number", "customer.ssn", "customer.email" // etc.
      ],
      "compliance_standards": ["PCI-DSS", "GDPR", "HIPAA"],
      "pipeline_id": "encrypt-production-v3"
    }

    # Send metadata to a separately managed audit endpoint
    _ = http_client(
      env("AUDIT_ENDPOINT_URL"),
      {"verb": "POST", "timeout": "2s"},
      audit_log
    )
```

## Pattern 3: Key Versioning & Decryption

To handle key rotation, the version of the key used for encryption should be stored alongside the encrypted data.

```yaml title="Storing the Key Version"
- mapping: |
    root.payment.card_number_encrypted = "%s:%s".format(
      env("KEY_VERSION"), // e.g., "v2"
      this.payment.card_number.encrypt_aes("gcm", env("CARD_ENCRYPTION_KEY_V2"))
    )
```

A corresponding decryption service would parse the version and retrieve the referenced key. Historical decryption still depends on retained keys, authorization, metadata integrity, and tested recovery procedures.

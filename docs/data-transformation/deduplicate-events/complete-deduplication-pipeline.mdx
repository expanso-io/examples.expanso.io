---
title: Complete Deduplication Pipeline
sidebar_label: Complete Pipeline
sidebar_position: 8
description: The complete event deduplication pipeline
keywords: [complete, deduplication, duplicate, idempotent]
contentArchetype: reference
executionStatus: architecture-only
operationalEvidence: not-assessed
localNavigation: true
uniqueTasks: [inspect-deduplicate-events-complete-deduplication-pipeline]
claimIds: []
claimsVerifiedBy: codex/content-claims-verifier-v1
claimsVerifiedAt: '2026-07-18'
claimsPolicyDigest: sha256:8d1c6ede95f42b16f4068c7997e7d46d38736d2f93bbf193543b2a52b4e340ab
---

import CodeBlock from '@theme/CodeBlock';
import pipelineYaml from '!!raw-loader!../../../examples/data-transformation/deduplicate-events-complete.yaml';
import CopyPipelineButton from '@site/src/components/CopyPipelineButton';

# Complete Pipeline

<CopyPipelineButton pipeline={pipelineYaml} />

This pipeline combines all deduplication techniques from this tutorial:

- **Hash-based deduplication** - Use content hash to detect exact duplicates
- **Fingerprint deduplication** - Detect near-duplicates with key field matching
- **ID-based deduplication** - Use event_id for idempotent processing
- **Time-windowed dedup** - Limit cache comparison to the configured time window

## Full Configuration

<CodeBlock language="yaml" title="deduplicate-events.yaml">
  {pipelineYaml}
</CodeBlock>

## Quick Test

```bash
# Send an event
curl -X POST http://localhost:8080/events \
  -H "Content-Type: application/json" \
  -d '{"event_id": "evt_001", "data": "hello"}'

# Send the same event again (will be deduplicated)
curl -X POST http://localhost:8080/events \
  -H "Content-Type: application/json" \
  -d '{"event_id": "evt_001", "data": "hello"}'

# Intended result: the repeated key is dropped
```

## Deploy

```bash
# Deploy to Expanso orchestrator
expanso-cli job deploy deduplicate-events.yaml

# Or run locally with expanso-edge
expanso-edge run --config deduplicate-events.yaml
```

## Download

<a
  href="/files/data-transformation/deduplicate-events.yaml"
  download
  className="button button--primary"
>
  Download deduplicate-events.yaml
</a>

## What's Next?

- [Troubleshooting](/data-transformation/deduplicate-events/troubleshooting) - Common issues and solutions
- [Normalize Timestamps](/data-transformation/normalize-timestamps) - Standardize time formats

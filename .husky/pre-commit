# Sync stage files first (generates YAML from .stages.ts files)
npm run sync-stages

# Add any newly generated files to the commit
git add examples/

# Run lint-staged for other validations
npx lint-staged